import { FoodsSearchPipe } from './foods-search.pipe';

describe('FoodsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FoodsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
