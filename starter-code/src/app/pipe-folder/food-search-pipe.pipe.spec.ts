import { FoodSearchPipePipe } from './food-search-pipe.pipe';

describe('FoodSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FoodSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
