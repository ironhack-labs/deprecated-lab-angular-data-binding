import { SearhPipePipe } from './searh-pipe.pipe';

describe('SearhPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearhPipePipe();
    expect(pipe).toBeTruthy();
  });
});
