import { HandleSumPipe } from './handle-sum.pipe';

describe('HandleSumPipe', () => {
  it('create an instance', () => {
    const pipe = new HandleSumPipe();
    expect(pipe).toBeTruthy();
  });
});
