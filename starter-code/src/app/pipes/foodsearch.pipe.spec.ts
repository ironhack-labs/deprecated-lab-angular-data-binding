import { FoodsearchPipe } from './foodsearch.pipe';

describe('FoodsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FoodsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
