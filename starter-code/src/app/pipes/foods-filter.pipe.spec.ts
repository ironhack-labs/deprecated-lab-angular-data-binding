import { FoodsFilterPipe } from './foods-filter.pipe';

describe('FoodsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FoodsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
