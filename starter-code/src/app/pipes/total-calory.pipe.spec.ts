import { TotalCaloryPipe } from './total-calory.pipe';

describe('TotalCaloryPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalCaloryPipe();
    expect(pipe).toBeTruthy();
  });
});
