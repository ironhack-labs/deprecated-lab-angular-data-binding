import { FoodSearcherPipe } from './food-searcher.pipe';

describe('FoodSearcherPipe', () => {
  it('create an instance', () => {
    const pipe = new FoodSearcherPipe();
    expect(pipe).toBeTruthy();
  });
});
