import { SearchbytermPipe } from './searchbyterm.pipe';

describe('SearchbytermPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbytermPipe();
    expect(pipe).toBeTruthy();
  });
});
