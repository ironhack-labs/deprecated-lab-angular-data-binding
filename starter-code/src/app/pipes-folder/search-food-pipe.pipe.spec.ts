import { SearchFoodPipePipe } from './search-food-pipe.pipe';

describe('SearchFoodPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFoodPipePipe();
    expect(pipe).toBeTruthy();
  });
});
