import { SearchablePipe } from './searchable.pipe';

describe('SearchablePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchablePipe();
    expect(pipe).toBeTruthy();
  });
});
