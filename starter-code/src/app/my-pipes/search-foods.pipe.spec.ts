import { SearchFoodsPipe } from './search-foods.pipe';

describe('SearchFoodsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFoodsPipe();
    expect(pipe).toBeTruthy();
  });
});
